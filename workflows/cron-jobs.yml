name: Daily Email Job

on:
  schedule:
    - cron: "15 10 * * *" # Өдөр бүр өглөөний 10:15 цагт ажиллана

jobs:
  send-email:
    runs-on: ubuntu-latest
    steps:
      - name: Call Vercel API
        run: curl -X POST https://resort-kappa.vercel.app/
