// import { auth } from "@/lib/auth";
"use client";

import { PropertyDetail } from "components/PropertyDetail/PropertyDetail";

const Home = () => {
  return (
    <div>
      <PropertyDetail />
    </div>
  );
};

export default Home;
